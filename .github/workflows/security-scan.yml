name: Automated Security Scan and Deploy

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  schedule:
    - cron: '0 0 * * *'  # Daily at midnight UTC
  workflow_dispatch:  # Manual trigger option

jobs:
  build-scan-push:
    runs-on: ubuntu-latest
    steps:
      # Checkout the repository
      - name: Checkout Repository
        uses: actions/checkout@v4

      # Set up Docker Buildx for building the image
      - name: Set Up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # Log in to Docker Hub using secrets
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # Build the base Docker image
      - name: Build Base Docker Image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/trivy-security-demo:base-${{ github.sha }} .

      # Set up Trivy
      - name: Set Up Trivy
        run: |
          sudo apt-get update -y
          sudo apt-get install -y wget
          wget https://github.com/aquasecurity/trivy/releases/download/v0.50.1/trivy_0.50.1_Linux-64bit.tar.gz
          tar zxvf trivy_0.50.1_Linux-64bit.tar.gz
          sudo mv trivy /usr/local/bin/
          trivy --version

      # Scan the image for vulnerabilities
      - name: Scan Docker Image with Trivy
        run: |
          trivy image --severity HIGH,CRITICAL --exit-code 1 \
          ${{ secrets.DOCKER_USERNAME }}/trivy-security-demo:base-${{ github.sha }} | tee trivy-scan.log
        continue-on-error: true

      # Generate Trivy report
      - name: Generate Trivy Report
        if: always()
        run: |
          trivy image --format json -o trivy-report.json \
          ${{ secrets.DOCKER_USERNAME }}/trivy-security-demo:base-${{ github.sha }}

      # Create final image with the report
      - name: Build Final Docker Image with Trivy Report
        run: |
          # Create a temporary container to copy the report into
          docker create --name temp-container ${{ secrets.DOCKER_USERNAME }}/trivy-security-demo:base-${{ github.sha }}
          docker cp trivy-report.json temp-container:/app/trivy-report.json
          # Commit the changes to a new image
          docker commit temp-container ${{ secrets.DOCKER_USERNAME }}/trivy-security-demo:${{ github.sha }}
          docker tag ${{ secrets.DOCKER_USERNAME }}/trivy-security-demo:${{ github.sha }} ${{ secrets.DOCKER_USERNAME }}/trivy-security-demo:latest
          docker rm temp-container

      # Upload Trivy report and scan log as artifacts
      - name: Upload Trivy Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: trivy-artifacts
          path: |
            trivy-report.json
            trivy-scan.log

      # Push to Docker Hub only if scan passes
      - name: Push Docker Image to Docker Hub
        if: success()
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/trivy-security-demo:${{ github.sha }}
          docker push ${{ secrets.DOCKER_USERNAME }}/trivy-security-demo:latest